local playerName = game.Players.LocalPlayer.Name
local function printPaths(instance)
    print(path)
    for _, child in ipairs(instance:GetChildren()) do
        printPaths(tchild, path .. "." .. child.Name)
    end
end
printPaths(StarterGui)
